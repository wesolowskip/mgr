[global]
name=scratch_${X}_${RW}_${W}_${ENABLE_NVLINKS}
xfer_type=$X
rw=$RW
bs=1M
size=2G
runtime=30
do_verify=0
num_threads=$W
enable_nvlinks=${ENABLE_NVLINKS}

[job1]
gpu_dev_id=0
directory=/scratch/shared/pwesolowski/gds_job_0
